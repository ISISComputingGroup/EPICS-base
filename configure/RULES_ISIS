ifeq ($(STATIC_BUILD),YES)
ifneq ($(findstring linux,$(EPICS_HOST_ARCH)),)
MYSQLLIB = mysqlcppconn-static mysqlclient
else
MYSQLLIB = mysqlcppconn8 mysqlcppconn
endif
else
ifneq ($(findstring linux,$(EPICS_HOST_ARCH)),)
MYSQLLIB = mysqlcppconn8
else
MYSQLLIB = mysqlcppconn8 mysqlcppconn
endif
endif

ioctests:

.PHONY: ioctests
